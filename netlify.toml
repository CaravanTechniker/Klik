[build]
  publish = "."
  command = ""

# Bez PWA cache – netlify headers (voliteľné, ale pomáha proti "starému dizajnu")
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
